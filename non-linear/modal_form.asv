clear; close all; clc;

A = [-2 1; 2 -3]; B = [1;2]; C = [2 1];
   
function [L, q_dot] = modal_sys(A,B,C)
% eigen vector and eigen values
   q = sym('Temirlan', [10 1], 'real');
   syms u;
   [V,E] =  eig(A);
   L = pinv(V)*A*V;
   B_new = pinv(V)*B;
   C_new = C*V;
   q_dot = L*q + B_new* u
end